plugins {
    id 'java'
}

dependencies {
    compileOnly "org.projectlombok:lombok:${rootProject.findProperty('lombokVersion')}"
    annotationProcessor "org.projectlombok:lombok:${rootProject.findProperty('lombokVersion')}"
}

test {
    useJUnitPlatform()
}